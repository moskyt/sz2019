%h1 Modul Závod

%p Každý účastník potřebuje kromě vybavení na celou akci také uzlovačku, zápisník a psací potřeby.

%table.table
  %tbody
    - Team::RACEPOINTS.each do |n, s|
      %tr
        %th= n
        %th= s[0]
        %td= @team.send("points_race_%02d" % n) || "---"
        %td= "/"
        %td= s[1]
        %td bodů